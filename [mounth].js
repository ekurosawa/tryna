{/*

// pages/[year]/[month].js
// ...
export async function getStaticPaths() {
    const months = getAllMonths()
    return {
      paths: months.map(month => {
        return {
          params: month,
        }
      }),
      fallback: false,
    }
  }
*/}